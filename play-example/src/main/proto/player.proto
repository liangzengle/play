syntax = "proto3";

package play.example.module.player.message;

message PlayerProto {
    int64 id = 1;
    string name = 2;
}

// gm指令执行结果
message GmResultProto {
    bool success = 1;
    string message = 2;
}