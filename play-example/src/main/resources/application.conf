app {
  reflection {
    //    packages-to-scan += "com.game.example"
  }

  db {
    # mysql mongodb memory
    repository = memory

    mysql {
      db = "play_example"
      username = "root"
      password = "123456"
    }

    mongodb {
      db = "play_example"
      auth = false
    }
  }
}

include "server.conf"